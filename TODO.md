## DashboardView.vue fix
- [ ] Inspect current `frontend/src/views/DashboardView.vue` for structural SFC issues.
- [ ] Replace file content with corrected single `<script setup>` SFC (remove duplicated stray script content).
- [ ] Re-run frontend lint/build (if available) to confirm `Invalid end tag` is gone.

